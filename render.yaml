services:
  - type: web
    name: command-center
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_RUNNER_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
